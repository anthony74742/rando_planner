{% extends 'base.html.twig' %}

{% block title %}Créer une randonnée{% endblock %}

{% block body %}
    <div class="min-h-screen bg-gray-50">
        {{ include('_nav.html.twig') }}

        <div class="max-w-3xl mx-auto px-4 py-8">
            {# ... existing code ... #}
            <div class="mb-6">
                <a href="{{ path('app_hike_index') }}" class="inline-flex items-center text-gray-600 hover:text-green-600 transition">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                    </svg>
                    Retour à la liste
                </a>
            </div>

            <div class="bg-white rounded-lg shadow-sm p-8">
                <h1 class="text-3xl font-bold text-gray-900 mb-2">Créer une nouvelle randonnée</h1>
                <p class="text-gray-600 mb-8">Partagez votre parcours préféré avec la communauté</p>

                {{ include('hike/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

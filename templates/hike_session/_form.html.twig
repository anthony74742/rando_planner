{{ form_errors(form) }}

{{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}

    {# Date #}
<div>
    {{ form_label(form.date, 'Date et heure', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-2'}}) }}
    {{ form_widget(form.date, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent', 'type': 'datetime-local'}}) }}
    {{ form_errors(form.date, {'attr': {'class': 'text-red-600 text-sm mt-1'}}) }}
</div>

{# Notes #}
<div>
    {{ form_label(form.notes, 'Notes', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-2'}}) }}
    {{ form_widget(form.notes, {
        'attr': {
            'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent',
            'rows': '4',
            'placeholder': 'Ajoutez des informations supplémentaires sur cette session...'
        }
    }) }}
    {{ form_errors(form.notes, {'attr': {'class': 'text-red-600 text-sm mt-1'}}) }}
    <p class="text-xs text-gray-500 mt-1">Facultatif - Conseils, points de rencontre, conditions météo prévues, etc.</p>
</div>

{# Hidden hike field #}
    {{ form_widget(form.hike) }}

    {# Submit #}
<div class="flex gap-3 pt-4">
    <button type="submit" class="flex-1 px-6 py-3 bg-green-600 text-white font-semibold rounded-lg hover:bg-green-700 transition shadow-sm">
        Créer la session
    </button>
    <a href="{{ path('app_hike_sessions') }}" class="flex-1 px-6 py-3 bg-gray-100 text-gray-700 font-semibold rounded-lg hover:bg-gray-200 transition text-center">
        Annuler
    </a>
</div>

{{ form_end(form) }}

<nav class="bg-white shadow-sm sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <div class="flex items-center">
                <a href="{{ path('app_home') }}" class="text-2xl font-bold text-green-600">
                    Randonnées
                </a>
            </div>

            <div class="flex items-center gap-4">
                {% if app.user %}
                    <a href="{{ path('app_hike_new') }}" class="px-4 py-2 text-green-600 hover:text-green-700 font-medium">
                        Créer
                    </a>
                    <a href="{{ path('app_hike_index') }}" class="px-4 py-2 text-gray-700 hover:text-gray-900">
                        Mes randonnées
                    </a>
                    <a href="{{ path('app_profile') }}" class="px-4 py-2 text-gray-700 hover:text-gray-900">
                        Profil
                    </a>
                    <a href="{{ path('app_logout') }}" class="px-4 py-2 text-gray-700 hover:text-gray-900">
                        Déconnexion
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="px-4 py-2 text-gray-700 hover:text-gray-900 font-medium">
                        Connexion
                    </a>
                    <a href="{{ path('app_register') }}" class="px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition">
                        Inscription
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
